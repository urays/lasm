This folder  contains three sections: 
    (1) lasm-ir 
    (2) lasm-ir-pseudo-operation 
    (3) lasm-ir-constant

You can use any browser to directly open the file with“.html”extension to view it. 
You can also use a reviewer that supports markdown format files. 
We recommend you to use the latter, because then you only need to open lasm-ir.md, 
and in lasm-ir.md you can click on the jump links to the other two files.